# Vision analyzer: Claude (приоритет) → Gemini (fallback)
# Claude имеет приоритет, Gemini — fallback при отсутствии или ошибке Claude

# Приоритет: используется первым для анализа скриншотов
ANTHROPIC_API_KEY=sk-ant-...

# Fallback: используется если Claude недоступен или падает
GEMINI_API_KEY=...
